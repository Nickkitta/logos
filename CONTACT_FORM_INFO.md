# 📬 Контактная форма - Информация

## Что реализовано

Контактная форма на сайте отправляет сообщения двумя способами:

1. **Telegram Bot** - мгновенные уведомления
2. **Email (Яндекс.Почта)** - для архива и официальной переписки

## Как это работает

### Пользователь заполняет форму:
- Имя (обязательно)
- Email или Telegram (хотя бы одно обязательно)
- Тема (необязательно)
- Сообщение (обязательно)

### Сервер обрабатывает:
- Валидирует данные
- Отправляет в Telegram (если настроен)
- Отправляет на Email (если настроен)
- Возвращает результат

### Результат:
- Если хотя бы один способ сработал - успех ✅
- Если оба не сработали - ошибка ❌

## Файлы

**API:**
- `server/api/contact.post.ts` - обработка формы

**Страница:**
- `pages/contact.vue` - контактная форма

**Конфигурация:**
- `.env` - настройки (не коммитится)
- `nuxt.config.ts` - runtime config

## Настройка

### Telegram:
См. `TELEGRAM_SETUP.md`

### Email:
См. `EMAIL_QUICK_START.md`

## Безопасность

- Все пароли и токены в `.env` (не коммитятся)
- Валидация данных на сервере
- Защита от пустых отправок
- Проверка email формата

## Продакшн

На продакшене (Vercel, Netlify и т.д.) добавь переменные окружения через панель управления:
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`
- `SMTP_USER`
- `SMTP_PASS`
- `SCHOOL_EMAIL`
