# 🚀 Деплой сайта на Vercel

## Почему Vercel?

- ✅ **Бесплатно** для личных проектов
- ✅ **Деплой за 5 минут** без настройки сервера
- ✅ **Автообновления** при push в GitHub
- ✅ **Встроенный SSL** (https://)
- ✅ **Свой домен** легко подключается
- ✅ **Все работает** - API, JSON, переменные окружения

## Пошаговая инструкция

### Шаг 1: Подготовь проект

1. Убедись, что проект в Git:
```bash
git init
git add .
git commit -m "Initial commit"
```

2. Создай репозиторий на GitHub:
   - Зайди на https://github.com/new
   - Создай новый репозиторий (можно приватный)
   - Следуй инструкциям для push

```bash
git remote add origin https://github.com/твой-username/твой-репозиторий.git
git branch -M main
git push -u origin main
```

### Шаг 2: Деплой на Vercel

1. Зайди на https://vercel.com
2. Нажми **Sign Up** и войди через GitHub
3. Нажми **Add New Project**
4. Выбери свой репозиторий
5. Vercel автоматически определит Nuxt 3
6. Нажми **Deploy**

Готово! Через 2-3 минуты сайт будет доступен.

### Шаг 3: Добавь переменные окружения

1. В Vercel перейди в **Settings** → **Environment Variables**
2. Добавь все переменные из `.env`:

```
TELEGRAM_BOT_TOKEN=твой-токен
TELEGRAM_CHAT_ID=твой-chat-id
SMTP_USER=твой-email@yandex.ru
SMTP_PASS=твой-пароль-приложения
SCHOOL_EMAIL=твой-email@yandex.ru
YANDEX_MAPS_API_KEY=твой-ключ
ADMIN_PASSWORD=твой-пароль
```

3. Нажми **Save**
4. Vercel автоматически передеплоит проект

### Шаг 4: Подключи свой домен

1. В Vercel перейди в **Settings** → **Domains**
2. Добавь свой домен (например: `school.ru`)
3. Vercel покажет DNS записи
4. Зайди в панель Reg.ru:
   - Найди управление DNS
   - Добавь A-запись или CNAME (как покажет Vercel)
5. Подожди 5-30 минут (пока DNS обновится)

Готово! Сайт доступен на твоем домене с SSL! 🎉

## Автообновления

Теперь при каждом push в GitHub:
```bash
git add .
git commit -m "Обновление"
git push
```

Vercel автоматически обновит сайт за 1-2 минуты!

## Альтернатива: Netlify

Если Vercel не подходит, попробуй Netlify:

1. Зайди на https://netlify.com
2. Sign Up через GitHub
3. **Add new site** → **Import from Git**
4. Выбери репозиторий
5. Build command: `npm run build`
6. Publish directory: `.output/public`
7. Deploy!

## Альтернатива: Reg.ru VPS (сложнее)

Если хочешь использовать свой хостинг на Reg.ru:

### Требования:
- VPS с Node.js 20+
- SSH доступ
- Минимум 1GB RAM

### Шаги:
1. Подключись по SSH
2. Установи Node.js 20+
3. Установи PM2: `npm install -g pm2`
4. Склонируй репозиторий
5. Установи зависимости: `npm install`
6. Создай `.env` файл
7. Собери проект: `npm run build`
8. Запусти: `pm2 start .output/server/index.mjs --name school`
9. Настрой Nginx как reverse proxy

**Это сложнее и дороже (~500₽/мес), поэтому рекомендую Vercel.**

## Рекомендация

Для твоего проекта **Vercel** - идеальный вариант:
- Бесплатно
- Просто
- Быстро
- Надежно
- Автообновления

Reg.ru можешь использовать только для домена (DNS), а хостинг - Vercel.

## Нужна помощь?

Если что-то не получается:
1. Проверь логи в Vercel Dashboard
2. Убедись, что все переменные окружения добавлены
3. Проверь, что `.env` не закоммичен в Git (должен быть в .gitignore)
