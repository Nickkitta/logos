# ⚠️ Исправление ошибки на продакшене

## Проблема
```
ENOENT: no such file or directory, open '/var/task/public/content/pages.json'
```

Код пытается читать JSON файлы, но на Vercel их нет.

## ✅ Что сделано
Исправлен `server/utils/storage.ts` - теперь правильно определяет окружение.

## 🔧 Что нужно сделать СРОЧНО:

### 1. Проверь переменные окружения KV в Vercel

Зайди в Vercel Dashboard → твой проект → **Settings** → **Environment Variables**

Должны быть эти переменные (добавляются автоматически при создании KV):
- ✅ `KV_REST_API_URL`
- ✅ `KV_REST_API_TOKEN`
- ✅ `KV_REST_API_READ_ONLY_TOKEN`
- ✅ `KV_URL`

**Если их НЕТ:**
1. Зайди в **Storage** → твоя KV база
2. Нажми **Connect Project**
3. Выбери свой проект
4. Vercel добавит переменные автоматически

### 2. Мигрируй данные в KV

Есть 2 способа:

#### Способ А: Через Vercel CLI (быстрее)

```bash
# Установи Vercel CLI
npm install -g vercel

# Залогинься
vercel login

# Подключись к проекту
vercel link

# Загрузи переменные окружения
vercel env pull .env.local

# Запусти миграцию
npm run migrate:kv
```

#### Способ Б: Вручную через админку (проще)

1. Открой свой сайт на Vercel
2. Зайди в админку `/admin`
3. Для каждой страницы (about, contacts, gallery):
   - Открой страницу в админке
   - Нажми "Сохранить"
   - Это сохранит данные в KV

### 3. Передеплой проект

После того как:
- ✅ Переменные KV добавлены
- ✅ Данные мигрированы
- ✅ Код обновлен (уже сделано)

Запуш изменения:
```bash
git add .
git commit -m "Fix KV storage detection"
git push
```

Vercel автоматически передеплоит проект.

### 4. Проверь что работает

1. Открой сайт на Vercel
2. Зайди в админку `/admin`
3. Попробуй изменить контент
4. Сохрани
5. Обнови страницу
6. Изменения должны сохраниться! ✅

## Почему это произошло?

Старый код использовал `lazy import` для KV, который не всегда успевал загрузиться.
Новый код импортирует KV сразу и правильно определяет окружение через `process.env.VERCEL`.

## Если всё еще не работает

1. Проверь логи в Vercel:
   - Dashboard → Deployments → последний деплой → View Function Logs

2. Убедись что KV база создана и подключена:
   - Dashboard → Storage → должна быть твоя KV база

3. Проверь переменные окружения:
   - Settings → Environment Variables → должны быть KV_* переменные

4. Попробуй пересоздать KV базу:
   - Storage → Create new KV → подключи к проекту
