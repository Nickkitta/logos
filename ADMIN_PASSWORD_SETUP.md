# 🔐 Настройка пароля для админ-панели

## Описание

Админ-панель и страница просмотра компонентов защищены паролем. Это предотвращает несанкционированный доступ к редактированию контента сайта.

## Быстрая настройка

### 1. Установите пароль в файле `.env`

Откройте файл `.env` (или создайте его из `.env.example`) и добавьте:

```env
ADMIN_PASSWORD=ваш_надежный_пароль
```

### 2. Перезапустите сервер

После изменения `.env` файла необходимо перезапустить dev-сервер:

```bash
npm run dev
```

## Пароль по умолчанию

Если переменная `ADMIN_PASSWORD` не установлена, используется пароль по умолчанию:

```
admin123
```

⚠️ **Важно:** Обязательно измените пароль по умолчанию перед развертыванием на продакшн!

## Защищенные страницы

- `/admin` - панель управления контентом
- `/components-showcase` - справочник компонентов

## Как войти

1. Перейдите на `/admin` или `/components-showcase`
2. Вы будете автоматически перенаправлены на страницу входа `/login`
3. Введите пароль
4. После успешной авторизации вы будете перенаправлены на запрошенную страницу

## Как выйти

В админ-панели есть кнопка "🚪 Выйти" в правом верхнем углу.

## Безопасность

### Рекомендации по паролю:

- Используйте минимум 12 символов
- Комбинируйте буквы (заглавные и строчные), цифры и спецсимволы
- Не используйте словарные слова
- Не используйте личную информацию

### Примеры надежных паролей:

```
K!r0_Adm1n_2024#Secure
MyS3cur3P@ssw0rd!2024
Adm1n#P@nel$2024
```

## Сессия

Авторизация сохраняется в `sessionStorage` браузера:
- Действует только в текущей вкладке браузера
- Автоматически удаляется при закрытии вкладки
- Не передается между вкладками

## Развертывание на продакшн

При развертывании на хостинг (Vercel, Netlify и т.д.):

1. Добавьте переменную окружения `ADMIN_PASSWORD` в настройках проекта
2. Убедитесь, что файл `.env` добавлен в `.gitignore` (уже добавлен)
3. Никогда не коммитьте реальный пароль в Git

### Пример для Vercel:

```bash
vercel env add ADMIN_PASSWORD
```

Или через веб-интерфейс:
1. Откройте настройки проекта
2. Перейдите в "Environment Variables"
3. Добавьте `ADMIN_PASSWORD` со значением вашего пароля

## Устранение проблем

### Забыли пароль?

1. Откройте файл `.env`
2. Измените значение `ADMIN_PASSWORD`
3. Перезапустите сервер

### Не можете войти?

1. Проверьте, что переменная `ADMIN_PASSWORD` установлена в `.env`
2. Убедитесь, что сервер был перезапущен после изменения `.env`
3. Проверьте консоль браузера на наличие ошибок
4. Попробуйте очистить `sessionStorage` в DevTools

### Автоматический выход

Если вы постоянно выходите из системы:
- Проверьте, что не используете режим инкогнито
- Убедитесь, что браузер не блокирует `sessionStorage`

## API Endpoint

Проверка пароля происходит через API endpoint:

```
POST /api/auth/login
Body: { "password": "ваш_пароль" }
```

Ответ при успехе:
```json
{
  "success": true,
  "message": "Авторизация успешна"
}
```

Ответ при ошибке (HTTP 401):
```json
{
  "success": false,
  "message": "Неверный пароль"
}
```

## Дополнительная безопасность

Для повышения безопасности можно:

1. Добавить ограничение по IP-адресам (на уровне хостинга)
2. Настроить rate limiting для защиты от брутфорса
3. Использовать HTTPS (обязательно на продакшн)
4. Регулярно менять пароль
5. Использовать VPN при работе с админкой из публичных сетей

## Техническая информация

- Middleware: `middleware/auth.ts`
- Страница входа: `pages/login.vue`
- API: `server/api/auth/login.post.ts`
- Хранилище: `sessionStorage` (клиент)
